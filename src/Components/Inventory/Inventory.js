import React from 'react';

const Inventory = () => {
    return (
        <div>
            <h2 className='review-items-container'>This is Inventory</h2>
        </div>
    );
};

export default Inventory;